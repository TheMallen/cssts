import * as expect from 'expect';

import Watcher from '../Watcher';

describe('Watcher', () => {
  it.skip('initializes without exploding', () => {
  });

  describe('watch', () => {
    beforeEach(() => {
    });

    it.skip('renders a .d.ts file when a css file changes', () => {
    });

    it.skip('deletes the .d.ts file when the corresponding css file is removed', () => {
    });
  });

  describe('configuration', () => {

  });
});
